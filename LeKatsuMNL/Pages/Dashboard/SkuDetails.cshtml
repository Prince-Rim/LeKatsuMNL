@page "{id:int?}"
@model LeKatsuMNL.Pages.Dashboard.SkuDetailsModel
@using LeKatsuMNL.Helpers
@{
    Layout = "_DashboardLayout";
    ViewData["Title"] = $"Inventory Items / {Model.SkuHeader?.ItemName ?? "SKU Details"}";
}

@section Styles {
    <style>
        .box-background {
            background-color: white;
            border-radius: 1rem;
            box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
            padding: 2rem;
        }

        .ingredient-table th {
            background-color: #f9f9f9;
            color: #999;
            font-size: 10px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            padding: 12px 20px;
            border-bottom: 1px solid #eee;
        }

        .ingredient-table td {
            padding: 16px 20px;
            border-bottom: 1px solid #f5f5f5;
        }

        /* Modal styling */
        .modal-backdrop {
            background-color: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(4px);
        }

        .custom-checkbox:checked {
            background-image: url("data:image/svg+xml,%3csvg viewBox='0 0 16 16' fill='white' xmlns='http://www.w3.org/2000/svg'%3e%3cpath d='M12.207 4.793a1 1 0 010 1.414l-5 5a1 1 0 01-1.414 0l-2-2a1 1 0 011.414-1.414L6.5 9.086l4.293-4.293a1 1 0 011.414 0z'/%3e%3c/svg%3e");
        }
    </style>
}

@section TitleActions {
    <div class="flex items-center space-x-3">
        @if (PermissionHelper.HasPermission(User, "SKU", 'U'))
        {
            <button type="submit" form="skuForm" class="px-8 py-2.5 bg-[#C41E24] text-white rounded-lg text-xs font-bold uppercase tracking-widest hover:bg-red-700 transition-all shadow-lg shadow-red-100 flex items-center">
                <i class="bi bi-floppy mr-2 text-sm"></i> Save Changes
            </button>
        }
    </div>
}

<div class="space-y-6 max-w-7xl mx-auto py-8 px-6">
    <!-- Product Header Section -->
    <div class="flex items-center space-x-8 mb-8">
        <div class="w-24 h-24 bg-gray-100 rounded-2xl flex items-center justify-center border border-gray-200 shadow-sm relative overflow-hidden group dark:bg-slate-700 dark:border-gray-700">
            <i class="bi bi-image text-3xl text-gray-300"></i>
            <div class="absolute inset-0 bg-black/5 opacity-0 group-hover:opacity-100 transition-all flex items-center justify-center">
                <i class="bi bi-pencil-square text-white text-xl"></i>
            </div>
        </div>
        <div class="flex-1 space-y-4">
            <h2 class="text-3xl font-extrabold text-gray-800 tracking-tight dark:text-white">@Model.SkuHeader?.ItemName: <span class="text-[#C41E24]">@Model.SkuHeader?.PackSize</span></h2>
            <div class="flex items-center space-x-4">
                <button type="button" class="px-6 py-2.5 bg-[#FFF5F5] text-[#C41E24] rounded-lg text-xs font-bold uppercase tracking-widest shadow-sm hover:bg-red-50 transition-all flex items-center border border-red-100/50">
                    <i class="bi bi-upload mr-2 text-sm"></i> Upload Image
                </button>
                <div class="flex-1 max-w-md">
                    <input type="text" placeholder="Enter Image URL" class="w-full bg-white border border-gray-200 rounded-lg px-4 py-2.5 text-sm focus:ring-2 focus:ring-[#C41E24]/5 focus:border-[#C41E24] outline-none transition-all placeholder:text-gray-400 shadow-sm dark:bg-slate-800 dark:placeholder:text-gray-500 dark:border-gray-700">
                </div>
                @if (PermissionHelper.HasPermission(User, "SKU", 'U'))
                {
                    <button type="button" class="p-2.5 bg-[#C41E24] text-white rounded-lg hover:bg-red-700 transition-all shadow-md">
                        <i class="bi bi-floppy text-lg"></i>
                    </button>
                }
            </div>
        </div>
    </div>

    <!-- Main Content Layout -->
    <form id="skuForm" method="post" class="grid grid-cols-12 gap-8">
        <!-- Left: Basic Information -->
        <div class="col-span-12 lg:col-span-5 space-y-6">
            <div class="space-y-5">
                <input type="hidden" asp-for="SkuHeader.SkuId" />
                
                <div>
                    <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Product Name</label>
                    <input type="text" asp-for="SkuHeader.ItemName" class="w-full bg-white border border-gray-200 rounded-xl px-5 py-3.5 text-base font-bold text-gray-800 focus:ring-2 focus:ring-[#C41E24]/5 focus:border-[#C41E24] outline-none transition-all shadow-sm dark:bg-slate-800 dark:text-white dark:border-gray-700">
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Category</label>
                        <input type="hidden" asp-for="SkuHeader.CategoryId" />
                        <input type="text" value="@(Model.SkuHeader?.Category?.CategoryName ?? "-")" readonly class="w-full bg-gray-50 border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none cursor-default dark:bg-slate-700/50 dark:text-white dark:border-gray-700">
                    </div>
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Sub-Class</label>
                        <input type="text" asp-for="SkuHeader.SubClass" placeholder="Sub-Class" class="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] transition-all dark:bg-slate-800 dark:text-white dark:border-gray-700">
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Packaging Type</label>
                        <select asp-for="SkuHeader.PackagingType" class="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] transition-all appearance-none cursor-pointer dark:bg-slate-800 dark:text-white dark:border-gray-700">
                            <option value="">Packaging Type</option>
                            <option value="Bottle">Bottle</option>
                            <option value="Pack">Pack</option>
                            <option value="Box">Box</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Packaging Unit</label>
                        <input type="text" asp-for="SkuHeader.PackagingUnit" placeholder="Packaging Unit" class="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] transition-all dark:bg-slate-800 dark:text-white dark:border-gray-700">
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Pack size</label>
                        <input type="text" asp-for="SkuHeader.PackSize" placeholder="Pack Size" class="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] transition-all dark:bg-slate-800 dark:text-white dark:border-gray-700">
                    </div>
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Unit of Measurement</label>
                        <input type="text" asp-for="SkuHeader.Uom" placeholder="UOM" class="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] transition-all dark:bg-slate-800 dark:text-white dark:border-gray-700">
                    </div>
                </div>

                <div>
                    <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Supplier</label>
                    <input type="text" asp-for="SkuHeader.Supplier" placeholder="Supplier" class="w-full bg-white border border-gray-200 rounded-xl px-5 py-3.5 text-sm font-bold text-gray-800 focus:ring-2 focus:ring-[#C41E24]/5 focus:border-[#C41E24] outline-none transition-all shadow-sm dark:bg-slate-800 dark:text-white dark:border-gray-700">
                </div>

                <div class="pt-2 flex items-center justify-between">
                    <div class="flex items-center space-x-6">
                        <label class="flex items-center space-x-2 cursor-pointer group">
                            <input type="checkbox" asp-for="SkuHeader.IsSellingPriceEnabled" class="w-5 h-5 rounded border-gray-300 text-[#C41E24] focus:ring-[#C41E24] transition-all dark:border-gray-600">
                            <span class="text-[10px] font-black text-gray-700 uppercase tracking-widest group-hover:text-[#C41E24] dark:text-gray-200">Selling Price</span>
                        </label>
                        <label class="flex items-center space-x-2 cursor-pointer group">
                            <input type="checkbox" asp-for="SkuHeader.IsReorderLevelEnabled" class="w-5 h-5 rounded border-gray-300 text-[#C41E24] focus:ring-[#C41E24] transition-all dark:border-gray-600">
                            <span class="text-[10px] font-black text-gray-700 uppercase tracking-widest group-hover:text-[#C41E24] dark:text-gray-200">Enable reorder level</span>
                        </label>
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-4 pt-2">
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Selling Price</label>
                        <input type="number" step="0.01" asp-for="SkuHeader.SellingPrice" placeholder="₱ 0.00" class="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] transition-all dark:bg-slate-800 dark:text-white dark:border-gray-700">
                    </div>
                    <div>
                        <label class="block text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 ml-1 dark:text-gray-500">Unit Cost</label>
                        <input type="number" step="0.01" asp-for="SkuHeader.UnitCost" placeholder="₱ 0.00" class="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] transition-all dark:bg-slate-800 dark:text-white dark:border-gray-700">
                    </div>
                </div>

                @if (PermissionHelper.HasPermission(User, "SKU", 'D'))
                {
                    <div class="pt-4">
                        <button type="button" class="px-8 py-3 bg-[#FFF5F5] text-[#C41E24] rounded-lg text-xs font-bold uppercase tracking-widest hover:bg-red-50 transition-all border border-red-100/50 shadow-sm flex items-center">
                            <i class="bi bi-trash3 mr-2 text-sm"></i> Archive this Product
                        </button>
                    </div>
                }
            </div>
        </div>

        <!-- Right: Ingredient Details -->
        <div class="col-span-12 lg:col-span-7 space-y-6">
            <div class="flex flex-col h-full bg-white rounded-2xl border border-gray-100 shadow-sm overflow-hidden dark:bg-slate-800 dark:border-gray-700">
                <div class="p-6 border-b border-gray-100 dark:border-gray-700">
                    <h3 class="text-sm font-black text-gray-700 uppercase tracking-widest mb-6 dark:text-gray-200">Ingredient Details</h3>
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-3">
                            @if (PermissionHelper.HasPermission(User, "SKU", 'U'))
                            {
                                <button type="button" onclick="openIngredientModal()" class="px-5 py-2.5 bg-[#C41E24] text-white rounded-lg text-xs font-bold uppercase tracking-widest hover:bg-red-700 transition-all shadow-md flex items-center">
                                    <i class="bi bi-plus-lg mr-2"></i> Add an ingredient
                                </button>
                                <button type="button" class="px-5 py-2.5 bg-[#C41E24] text-white rounded-lg text-xs font-bold uppercase tracking-widest hover:bg-red-700 transition-all shadow-md flex items-center">
                                    <i class="bi bi-arrow-repeat mr-2"></i> Update
                                </button>
                            }
                        </div>
                        @if (PermissionHelper.HasPermission(User, "SKU", 'D'))
                        {
                            <button type="button" class="px-5 py-2.5 bg-white border border-[#C41E24] text-[#C41E24] rounded-lg text-xs font-bold uppercase tracking-widest hover:bg-red-50 transition-all flex items-center dark:bg-slate-800">
                                <i class="bi bi-file-earmark-x mr-2"></i> Detach SKU
                            </button>
                        }
                    </div>
                </div>

                <div class="flex-1 overflow-x-auto">
                    <table class="w-full text-left ingredient-table">
                        <thead>
                            <tr>
                                <th>Item Name</th>
                                <th class="w-32 text-center">Quantity</th>
                                <th class="w-32 text-center">UOM</th>
                                <th class="w-16"></th>
                            </tr>
                        </thead>
                        <tbody id="ingredientItemsList">
                            @if (Model.SkuHeader?.SkuRecipes != null)
                            {
                                var recipes = Model.SkuHeader.SkuRecipes.ToList();
                                for (int i = 0; i < recipes.Count; i++)
                                {
                                    <tr class="ingredient-row" data-com-id="@recipes[i].ComId">
                                        <td>
                                            <input type="hidden" name="SkuHeader.SkuRecipes[@i].ComId" value="@recipes[i].ComId" />
                                            <div class="flex items-center space-x-4">
                                                <div class="w-12 h-12 bg-gray-200 rounded-lg flex-shrink-0"></div>
                                                <span class="font-bold text-gray-800 dark:text-white">@recipes[i].CommissaryInventory?.ItemName</span>
                                            </div>
                                        </td>
                                        <td>
                                            <input type="text" name="SkuHeader.SkuRecipes[@i].QuantityNeeded" value="@recipes[i].QuantityNeeded" class="w-full bg-white border border-gray-200 rounded-lg px-3 py-2 text-center text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] dark:bg-slate-800 dark:text-white dark:border-gray-700">
                                        </td>
                                        <td>
                                            <select name="SkuHeader.SkuRecipes[@i].Uom" class="w-full bg-white border border-gray-200 rounded-lg px-3 py-2 text-center text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] appearance-none cursor-pointer dark:bg-slate-800 dark:text-white dark:border-gray-700">
                                                <option value="Grams" selected="@(recipes[i].Uom == "Grams")">Grams</option>
                                                <option value="ML" selected="@(recipes[i].Uom == "ML")">ML</option>
                                                <option value="PCS" selected="@(recipes[i].Uom == "PCS")">PCS</option>
                                            </select>
                                        </td>
                                        <td class="text-right pr-4">
                                            @if (PermissionHelper.HasPermission(User, "SKU", 'U'))
                                            {
                                                <button type="button" onclick="removeIngredientRow(this)" class="text-gray-400 hover:text-red-500 transition-colors dark:text-gray-500">
                                                    <i class="bi bi-trash3"></i>
                                                </button>
                                            }
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </form>
</div>

<!-- Add Ingredient Modal -->
<div id="ingredientModal" class="fixed inset-0 z-[100] hidden overflow-y-auto">
    <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
        <div class="fixed inset-0 transition-opacity modal-backdrop" onclick="closeIngredientModal()"></div>

        <span class="hidden sm:inline-block sm:align-middle sm:h-screen">&#8203;</span>

        <div class="inline-block align-bottom bg-white rounded-2xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-xl sm:w-full border border-gray-100 dark:bg-slate-800 dark:border-gray-700">
            <div class="p-8">
                <div class="flex items-center justify-between mb-8">
                    <h3 class="text-sm font-black text-gray-700 uppercase tracking-widest dark:text-gray-200">@(Model.SkuHeader?.ItemName ?? "Product") - Add ingredient</h3>
                    <button onclick="closeIngredientModal()" class="text-gray-400 hover:text-gray-600 transition-colors dark:text-gray-500">
                        <i class="bi bi-x-lg text-lg"></i>
                    </button>
                </div>

                <div class="relative mb-6">
                    <i class="bi bi-search absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 dark:text-gray-500"></i>
                    <input type="text" id="modalSearch" onkeyup="filterItems()" placeholder="Search ingredient" class="w-full bg-gray-50 border border-gray-200 rounded-xl pl-12 pr-12 py-3.5 text-sm font-bold text-gray-800 focus:ring-2 focus:ring-[#C41E24]/5 focus:border-[#C41E24] outline-none transition-all placeholder:text-gray-400 dark:bg-slate-700/50 dark:text-white dark:placeholder:text-gray-500 dark:border-gray-700">
                    <i class="bi bi-grid-3x3-gap absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 dark:text-gray-500"></i>
                </div>

                <div class="max-h-[400px] overflow-y-auto pr-2 space-y-2 custom-scrollbar">
                    @if (Model.AvailableItems != null && Model.AvailableItems.Any())
                    {
                        @foreach (var item in Model.AvailableItems)
                        {
                            <div class="item-row flex items-center space-x-4 p-4 rounded-xl hover:bg-gray-50 transition-all border border-transparent hover:border-gray-100 cursor-pointer group dark:hover:bg-slate-700/50" onclick="toggleItemSelection(this)">
                                <div class="flex items-center justify-center w-6 h-6 border-2 border-gray-300 rounded group-hover:border-[#C41E24] transition-all checkbox-container dark:border-gray-600">
                                    <input type="checkbox" class="hidden item-checkbox" value="@item.ComId" data-name="@item.ItemName">
                                    <i class="bi bi-check-lg text-white text-xs opacity-0"></i>
                                </div>
                                <div class="w-12 h-12 bg-gray-200 rounded-lg flex-shrink-0 overflow-hidden">
                                </div>
                                <span class="flex-1 font-bold text-gray-800 dark:text-white">@item.ItemName</span>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="p-6 text-center text-gray-400 font-medium text-sm italic dark:text-gray-500">
                            No commissary items available. Please add items to the inventory first.
                        </div>
                    }
                </div>
            </div>
            <div class="p-6 bg-gray-50 border-t border-gray-100 flex justify-end space-x-3 dark:bg-slate-700/50 dark:border-gray-700">
                <button onclick="closeIngredientModal()" class="px-6 py-2.5 text-xs font-black text-gray-500 uppercase tracking-widest hover:text-gray-700 transition-colors dark:text-gray-400">Cancel</button>
                <button onclick="addSelectedItems()" class="px-8 py-2.5 bg-[#C41E24] text-white rounded-lg text-xs font-bold uppercase tracking-widest hover:bg-red-700 transition-all shadow-lg shadow-red-100">Confirm Selection</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function openIngredientModal() {
            document.getElementById('ingredientModal').classList.remove('hidden');
            document.body.style.overflow = 'hidden';
        }

        function closeIngredientModal() {
            document.getElementById('ingredientModal').classList.add('hidden');
            document.body.style.overflow = 'auto';
        }

        function toggleItemSelection(element) {
            const checkboxContainer = element.querySelector('.checkbox-container');
            const checkIcon = element.querySelector('.bi-check-lg');
            const text = element.querySelector('span');
            
            if (checkboxContainer.classList.contains('bg-[#C41E24]')) {
                checkboxContainer.classList.remove('bg-[#C41E24]', 'border-[#C41E24]');
                checkboxContainer.classList.add('border-gray-300');
                checkIcon.classList.replace('opacity-100', 'opacity-0');
                text.classList.remove('text-[#C41E24]');
            } else {
                checkboxContainer.classList.add('bg-[#C41E24]', 'border-[#C41E24]');
                checkboxContainer.classList.remove('border-gray-300');
                checkIcon.classList.replace('opacity-0', 'opacity-100');
                text.classList.add('text-[#C41E24]');
            }
        }

        function filterItems() {
            const input = document.getElementById('modalSearch');
            const filter = input.value.toUpperCase();
            const rows = document.querySelectorAll('.item-row');

            rows.forEach(row => {
                const text = row.querySelector('span').innerText;
                if (text.toUpperCase().indexOf(filter) > -1) {
                    row.style.display = "";
                } else {
                    row.style.display = "none";
                }
            });
        }

        function addSelectedItems() {
            const tableBody = document.getElementById('ingredientItemsList');
            const selectedItems = document.querySelectorAll('.item-row .checkbox-container.bg-\\[\\#C41E24\\]');
            
            selectedItems.forEach(container => {
                const row = container.closest('.item-row');
                const checkbox = row.querySelector('.item-checkbox');
                const comId = checkbox.value;
                const itemName = checkbox.getAttribute('data-name');
                
                // Check if already in table
                if (!document.querySelector(`tr[data-com-id="${comId}"]`)) {
                    const index = tableBody.querySelectorAll('tr').length;
                    const newRow = `
                        <tr class="ingredient-row" data-com-id="${comId}">
                            <td>
                                <input type="hidden" name="SkuHeader.SkuRecipes[${index}].ComId" value="${comId}" />
                                <div class="flex items-center space-x-4">
                                    <div class="w-12 h-12 bg-gray-200 rounded-lg flex-shrink-0"></div>
                                    <span class="font-bold text-gray-800 dark:text-white">${itemName}</span>
                                </div>
                            </td>
                            <td>
                                <input type="text" name="SkuHeader.SkuRecipes[${index}].QuantityNeeded" value="1" class="w-full bg-white border border-gray-200 rounded-lg px-3 py-2 text-center text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] dark:bg-slate-800 dark:text-white dark:border-gray-700">
                            </td>
                            <td>
                                <select name="SkuHeader.SkuRecipes[${index}].Uom" class="w-full bg-white border border-gray-200 rounded-lg px-3 py-2 text-center text-sm font-bold text-gray-800 outline-none focus:border-[#C41E24] appearance-none cursor-pointer dark:bg-slate-800 dark:text-white dark:border-gray-700">
                                    <option value="Grams">Grams</option>
                                    <option value="ML">ML</option>
                                    <option value="PCS">PCS</option>
                                </select>
                            </td>
                            <td class="text-right pr-4">
                                <button type="button" onclick="removeIngredientRow(this)" class="text-gray-400 hover:text-red-500 transition-colors dark:text-gray-500">
                                    <i class="bi bi-trash3"></i>
                                </button>
                            </td>
                        </tr>
                    `;
                    tableBody.insertAdjacentHTML('beforeend', newRow);
                }
            });
            
            closeIngredientModal();
            renumberRows();
        }

        function removeIngredientRow(button) {
            button.closest('tr').remove();
            renumberRows();
        }

        function renumberRows() {
            const rows = document.querySelectorAll('#ingredientItemsList tr.ingredient-row');
            rows.forEach((row, index) => {
                const comIdInput = row.querySelector('input[name*=".ComId"]');
                const qtyInput = row.querySelector('input[name*=".QuantityNeeded"]');
                const uomSelect = row.querySelector('select[name*=".Uom"]');
                
                if (comIdInput) comIdInput.name = `SkuHeader.SkuRecipes[${index}].ComId`;
                if (qtyInput) qtyInput.name = `SkuHeader.SkuRecipes[${index}].QuantityNeeded`;
                if (uomSelect) uomSelect.name = `SkuHeader.SkuRecipes[${index}].Uom`;
            });
        }
    </script>
}
